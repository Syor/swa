spring:
  application:
    name: PetrService
  cloud:
    zookeeper:
      connect-string: 35.247.47.255:2181
      discovery:
        enabled: true
  datasource:
    url: jdbc:postgresql://localhost:5432/postgres
    username: postgres
    password: postgrespw
    driver-class-name: org.postgresql.Driver
